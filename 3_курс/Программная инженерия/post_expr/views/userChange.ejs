<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <title>Изменение пользователя</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
</head>

<body>
    <h2 class="page-head-title">Измените поля</h2>
    <div>
        <form class="page-head" action="POST">
            <div class="addBox">
                <div class="addBoxItem">
                    <p>Логин</p>
                    <input 
                        class="page-head-elements"
                        type="text" 
                        name="name" 
                        value="<%= result[0]['username'] %>" 
                        placeholder="Введите название"
                        required
                    />
                </div>
                <div class="addBoxItem">
                    <p>Роль</p>
                    <select class="page-head-elements" name="role" id="status_choose">
                        <% for (let i = 0; i < result[1].length; i++) { %>
                            <option value="<%= result[1][i].role_id %>" <% if (result[1][i].role_id == result[0].role) { %>selected<% } %> > <%= result[1][i].role_name %></option>
                        <% } %>
                    </select>
                </div>
                <div class="addBoxItem">
                    <p>Зал</p>
                    <select class="page-head-elements" name="gym" id="status_choose">
                        <% for (let i = 0; i < result[2].length; i++) { %>
                            <option value="<%= result[2][i].branch_id %>" <% if (result[2][i].branch_id == result[0].branch) { %>selected<% } %> > <%= result[2][i].branch_name %></option>
                        <% } %>
                    </select>
                </div>
                <div class="addBoxItem">
                    <button 
                        type="submit" 
                        class="page-head-elements"
                        formaction="/userChange/:<%= result[0].user_id %>" 
                        formmethod="post">
                        Изменить
                    </button>
                </div>
            </div>
        </form>
        <form action="GET">
            <button 
                class="del"
                formaction="/" 
                formmethod="get">
                На главную
            </button>
        </form>
    </div>
</body>
</html>